class Transactions {
  String id;
  String title;
  double amount;
  DateTime date;
  Transactions({
    required this.id,
    required this.title,
    required this.amount,
    required this.date,
  });
}
